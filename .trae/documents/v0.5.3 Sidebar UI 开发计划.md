我将按照 TDD 模式完成 v0.5.3 (侧边栏 UI) 的开发。

### 1. 任务拆解
1.  **创建 Sidebar 组件测试 (`Sidebar.test.tsx`)**
    *   测试组件渲染与初始状态。
    *   测试“开始排查”与“结束排查”的模式切换。
    *   测试输入框与消息列表的交互。
2.  **实现 Sidebar 组件 (`Sidebar.tsx`)**
    *   实现基础布局：Header, MessageList, Footer。
    *   实现状态管理：IsInvestigating, Messages, Input。
    *   提供对外回调：`onStart`, `onStop`, `onSendMessage` (预留)。
3.  **集成到 App (`App.tsx`)**
    *   调整主布局为 Flex Row (左侧 LayoutManager，右侧 Sidebar)。
    *   添加 Sidebar 的显示/隐藏控制状态。
    *   确保 Sidebar 切换时触发终端 Resize (fit)。

### 2. 测试策略
*   使用 `vitest` + `@testing-library/react` 进行组件测试。
*   通过 `fireEvent` 模拟用户点击和输入。
*   验证 UI 元素（按钮、输入框、消息内容）的存在与状态变化。

### 3. 执行计划
我将先创建测试文件，然后实现组件，最后进行集成。
