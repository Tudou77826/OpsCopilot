# v1.8.9.2

## 修复

- 修复定位助手和 AI 问答面板收起后仍保留空白区域的问题。关闭状态现在会完整释放面板宽度，不再挤占终端工作区。
- 移除定位助手空状态中的预设问题示例，让用户直接输入实际故障现象。

## 验证

- `npm run build`
- `npm test -- --run src/components/Sidebar/Sidebar.test.tsx --reporter=dot --maxWorkers=1 --pool=threads`
- `npm test -- --run src/components/Sidebar/TroubleshootingPanel.test.tsx --reporter=dot --maxWorkers=1 --pool=threads`
