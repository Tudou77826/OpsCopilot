# v1.8.9.4

## 修复

- 修复已保存会话重命名后，连接成功时显示名称被主机地址覆盖的问题。自定义名称现在会在自动保存和后续连接中保持不变，主机地址仍用于连接、去重和安全策略匹配。
- 修复设置页保存时未持久化终端高亮规则的问题。

## 优化

- 设置保存成功后保持当前页面打开，并在关闭存在未保存改动的设置页前进行确认。

## 验证

- `go test ./...`
- `npm test -- --run --reporter=dot --maxWorkers=1 --pool=threads`
- `npm run build`
