# v1.6.9

## 更新内容

- 优化“新建连接”弹窗布局，将连接描述、AI 分析和连接列表分区重排，减少内容互相挤压。
- 新增跳板机连接模板，一键填入常用跳板机/目标主机账号信息格式。
- 连接描述输入框禁用拼写校验，避免账号、密码、IP 等内容被浏览器误标。
- 解析出多条连接后自动压缩输入区，把空间让给连接配置表单。
- 修复多连接结果展开时跳板机字段显示不全的问题。

## 验证

- `npm test -- SmartConnectModal.test.tsx --run`
- `npm run build`
