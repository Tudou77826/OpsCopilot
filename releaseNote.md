feat: 支持会话文件夹结构并添加rootPassword字段

- 在ConnectConfig中添加rootPassword字段支持
- 修改会话数据结构支持文件夹层级
- 在前端添加配置键名转换逻辑(snake_case转camelCase)

----------------

fix(样式): 修复嵌套flex布局中的滚动问题

为多个面板组件添加minHeight: 0解决flex容器滚动问题
在根元素和侧边栏内容区域添加overflow: hidden防止页面滚动

-------------------


feat(ai): 停止清理AI响应以支持Markdown格式

修改AskWithContext方法不再清理JSON响应，改为直接返回原始响应以支持Markdown格式。同时更新了默认提示模板和配置文件中的qa_prompt，统一使用Markdown格式进行响应。


-------------

feat(终端): 添加标签页右键菜单支持复制和重命名功能

实现终端标签页的右键菜单功能，支持复制当前会话和重命名标签页
后端新增DuplicateSession方法用于复制会话配置
前端添加上下文菜单UI和处理逻辑



-----


feat(终端广播): 实现基于标签页开关的实时终端广播功能

- 新增广播模式开关及状态管理
- 在设置面板中添加广播模式切换控件
- 移除旧的广播输入栏组件
- 在终端标签页和网格视图中添加广播状态指示器
- 优化广播性能，避免空会话ID广播
- 更新文档说明广播功能实现细节

---------------

feat(故障排查): 新增结构化故障排查功能

- 在后端添加 AskTroubleshoot 方法处理结构化故障排查请求
- 实现 JSON 格式的故障排查步骤和命令输出
- 前端组件支持解析和展示结构化响应
- 添加默认故障排查提示模板
- 改进 JSON 响应清理逻辑以处理多行代码块

-----------

feat(terminal): 添加终端输入清理功能并优化会话记录

新增终端输入清理功能，移除ANSI转义序列和垃圾字符，优化会话记录的可读性
添加相关测试用例确保清理功能正确性
重构前端时间线工具函数，提取公共逻辑
更新会话记录生成逻辑，使用清理后的输入

-----------------

refactor(session_recorder): 重构终端输入处理逻辑，使用LineBuffer处理ANSI序列

将终端输入处理逻辑从简单的清理和聚合改为使用LineBuffer结构，能够正确处理ANSI转义序列、退格键和光标移动。移除旧的CleanInput方法，改为在recorder中维护每个会话的LineBuffer实例。

新增LineBuffer实现，包含完整的终端输入处理能力，支持光标移动、删除、插入等操作。同时更新测试用例验证各种终端输入场景。

--------------



refactor(广播记录): 使用专用方法处理广播输入以支持去重

将广播输入记录逻辑从App.Broadcast移动到Recorder.AddBroadcastInput方法
新增对广播输入的去重处理，按提交行分组会话


---------


